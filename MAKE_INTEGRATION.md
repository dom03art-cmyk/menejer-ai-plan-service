# Make.com scenario-г шинэ сервистэй холбох дизайн

Энэ баримт нь `mongol-business-plan` скилийн чанар/арга зүйтэй ижил түвшний
(судалгаатай, тоон үзүүлэлттэй, графиктай, MNS 5140 стандарт дагасан,
25-30 хуудас) бизнес төсөл-ийг **таны Facebook Messenger бот дээр
автоматаар** үйлдвэрлэх зорилгоор одоогийн "Менежер AI - Захиалга хүлээн
авах" (scenario ID 6206912) Make.com scenario-г хэрхэн өөрчлөх тухай
дэлгэрэнгүй төлөвлөгөө.

**Энэ бол зөвхөн дизайн/төлөвлөгөө — одоогийн ажиллаж байгаа scenario-д
ЯМАР Ч өөрчлөлт хараахан ороогүй байна.** Учир нь:

1. Доорх дизайн нь `menejer-ai-plan-service`-г бодит URL дээр deploy
   хийсэн байхыг шаарддаг (одоогоор танд байхгүй — README.md-ийн 1-р
   хэсгийг үзнэ үү).
2. Энэ бол одоо **бодит, төлбөр төлсөн хэрэглэгчидтэй ажиллаж байгаа
   production bot** тул URL бэлэн болоод, доорх дизайныг та зөвшөөрсний
   дараа л би live scenario дээр өөрчлөлтийг тусгайлан хийнэ.

---

## 1. Яагаад одоогийн бүтэц хангалтгүй вэ

Одоогийн scenario-д (module 21, 36) төлөвлөгөөг **нэг л Claude API
дуудлагаар** (`max_tokens: 8192`), нэг л prompt-оор бичүүлдэг. Үүний
хязгаарлалт:

- Нэг дуудлагад ойролцоогоор 5-8 хуудас орчим л агуулга гарна (25-30
  хуудасны шаардлагад хүрэхгүй).
- Claude өөрөө тоо тооцоолохгүй тул НПВ/ДТОХ/BEP зэрэг тоонууд алдаатай
  эсвэл "тааж" гарах эрсдэлтэй.
- График, хүснэгт, TAM/SAM/SOM зэрэг бодит судалгаа шаардсан тооцоо
  байхгүй.
- Гаралт нь Google Docs-оор хийгдсэн энгийн текст баримт — MNS 5140
  стандартын формат, автомат Гарчиг (TOC) байхгүй.

## 2. Шинэ бүтэц — тоймоор

Skill-ийн өөрийн зөвлөмжийн дагуу (`SKILL.md`-ийн "Бүлэг тус бүрээр
модуль болгож боловсруулах горим" хэсэг): **5 бүлгийг тус тусад нь Claude
API дуудлагаар бичүүлж**, эцэст нь тооцоо/график/docx угсралтыг шинэ
Node сервисээр хийлгэнэ.

```
[9. GetRecord] → [Финанс тооцоо: /calc/finance, /calc/payroll]
                        ↓
[Claude: НЭГ+ХОЁР бүлэг]  (нэг дуудлага, хамтад нь)
                        ↓
[Claude: ГУРАВ бүлэг]           (өмнөх бүлгүүдийн товч контекст дамжина)
                        ↓
[Claude: ДӨРӨВ бүлэг]           (тооцоолсон бодит тоог prompt-д шигтгэнэ)
                        ↓
[Claude: ТАВ бүлэг]
                        ↓
[Бүх бүлгийг sections[] массив болгож угсрах] → [POST /docx] → Word файл
                        ↓
[Facebook Messenger-ээр илгээх] (одоогийн multipart HTTP модультай ижил)
```

Google Docs create/export 2 модулийг (23-24, 38-39) бүрмөсөн хасаж,
оронд нь ганц `POST /docx` HTTP модуль тавина — энэ нь одоогийн бүтцээс
**илүү энгийн** (алхам цөөрнө), учир нь Google Docs холболт шаардлагагүй
болно.

## 3. Модуль тус бүрийн дизайн

### 3.0. Санхүүгийн тооцоо (шинэ, эхэнд орно)

Хэрэглэгчийн ярилцлагаас цуглуулсан мэдээллээс (`{{9.history}}`) хөрөнгө
оруулалт, орлого зэрэг тоог Claude-ээр **JSON болгож гаргуулах** нэмэлт
жижиг алхам хэрэгтэй (учир нь одоогийн ярилцлага чөлөөт текст хэлбэртэй):

- **Шинэ модуль (HTTP → Claude):** "Ярилцлагын түүхээс дараах тоон
  мэдээллийг ЗӨВХӨН JSON форматаар гарга: `cash_flows` (5 жилийн, 0-р
  жил = сөрөг эхний хөрөнгө оруулалт), `positions` (ажлын байр,
  цалин), `break_even` (тогтмол зардал/нэгжийн үнэ/хувьсах зардал)."
  — хариултыг `json:ParseJSON`-ээр задална.
- **Дараа нь 2 HTTP модуль:** `POST {{SERVICE_URL}}/calc/finance` ба
  `POST {{SERVICE_URL}}/calc/payroll`, дээрх JSON-г бие даан дамжуулна.
  Header: `x-api-key: {{API_KEY}}`.
- Гарсан НПВ/ДТОХ/Payback/BEP/ROI/цалингийн тоог дараагийн бүх Claude
  дуудлагын prompt-д "БОДИТ ТООЦООЛСОН ҮЗҮҮЛЭЛТ (үүнийг ашигла, дахин бүү
  тоол)" гэсэн хэсэгт хатуу оруулна — ингэснээр Claude тоо "зохиохгүй".

### 3.1-3.4. Бүлэг тус бүрийн Claude дуудлага (4 модуль)

Одоогийн module 21/36-тай ижил бүтэцтэй (`http:MakeRequest`,
`api.anthropic.com/v1/messages`), гэхдээ:

- `system` prompt тус бүрийг `SKILL.md`-ийн "Канончилсон бүтэц" хэсэгт
  (НЭГ-ТАВ) заасан дэд бүлгүүдийн жагсаалтаар **тухайн бүлгийн бүрэн
  дэд-бүтцийг** зааж өгнө (жишээ нь ДӨРӨВ-т: 4.1 CAPEX/OPEX, 4.2
  Ашигт ажиллагаа, 4.3 Өртгийн тооцоо, 4.4 ХН зардал, 4.5 Үйл
  ажиллагааны зардал).
- `max_tokens` тус бүрд 4096-6144 (нэг бүлэгт хангалттай, нийт 4 удаагийн
  дуудлага нийлээд 25-30 хуудас гарна).
- **Судалгаа хийх боломж:** Anthropic Messages API-ийн `web_search`
  server tool-г `tools` параметрт нэмбэл (жишээ доор), Claude өөрөө
  интернэтээс Монголын зах зээлийн бодит тоо/жишиг хайж олоод бичихэд
  ашиглана (ХОЁР, ДӨРӨВ бүлэгт хамгийн чухал — зах зээлийн хэмжээ,
  салбарын жишиг үзүүлэлт). Жишээ нэмэлт `dataStructureBodyContent`:
  ```json
  "tools": [{"type": "web_search_20250305", "name": "web_search", "max_uses": 3}]
  ```
  Энэ нь дуудлагын хугацааг 10-30 секундээр нэмэгдүүлдэг тул
  `timeout: 180` (одоо байгаа шиг) хадгална.
- Өмнөх бүлгүүдийн текстийг дараагийн дуудлагад бүрэн биш, зөвхөн
  **товч резюме** (2-3 өгүүлбэр) хэлбэрээр дамжуулна (жишээ нь "1-2
  бүлэгт бичсэн товч: ...") — ингэснээр prompt хэт урт болохгүй, зардал
  хэмнэгдэнэ.
- Гарсан хариултыг `json:ParseJSON`-оор задлаад, дараагийн модульд
  зориулж Data Store-д (эсвэл scenario variable-д) хадгална.

### 3.5. sections[] массив угсрах

Make.com-ийн **"Set variable" / "Array aggregator"** ашиглан 4 бүлгийн
текстийг нэг `sections` массив болгоно. Учир нь `/docx` нь typed-block
(`h1/h2/h3/p/bullet/table/chart`) хэлбэртэй массив хүлээж авдаг тул
Claude-ийн буцаасан чөлөөт текстийг эхлээд **энгийн парсер алгоритмаар**
(мөр мөрөөр нь — "ТОМ ҮСГЭЭР бичсэн мөр → h2", "1.1 гэж эхэлсэн мөр →
h3", бусад → p) хөрвүүлэх шаардлагатай. Хамгийн энгийн шийдэл: Claude-д
шууд **JSON блок массив** (яг `/docx`-ийн sections бүтэцтэй) буцаахыг
даалгах — ингэвэл дундын парс хэрэггүй болно:

> "Хариултаа ЗӨВХӨН дараах JSON бүтэцтэй массив байдлаар буцаа:
> `[{"type":"h2","text":"..."},{"type":"p","text":"..."},{"type":"table","headers":[...],"rows":[[...]]}]`.
> Markdown ашиглахгүй, тайлбар текст нэмэхгүй, зөвхөн цэвэр JSON."

Энэ хандлага нь одоогийн 12/21/36 модулиудын "зөвхөн энгийн текст бич"
зааврыг сольж, шууд бүтэцтэй өгөгдөл гаргуулдаг тул хамгийн найдвартай.

Тооцооллын хүснэгт (`/calc/finance`, `/calc/payroll`-ийн үр дүн) болон
2-3 chart блокийг (орлогын таамаглал — line chart, зардлын бүтэц — pie
chart, мөнгөн урсгал — bar chart) энэ массивт Claude-ийн буцаасан
блокуудын хооронд гараар (Make.com-ийн Set variable модулиар) шигтгэнэ.

### 3.6. `POST /docx` дуудаж, Facebook-руу илгээх

Одоогийн module 25/40-тэй **яг ижил** multipart бүтэц ашиглана — зөвхөн
эх сурвалж нь `google-docs:exportADocument`-ийн оронд шинэ
`http:MakeRequest`-ийн binary response болно:

```
url: {{SERVICE_URL}}/docx
method: POST
headers: [{ "x-api-key": "{{API_KEY}}" }]
body (dataStructure): { "meta": {...}, "sections": {{assembled_array}} }
```

Хариу нь шууд `.docx` binary тул `filedata`-д `{{thisModule.data}}`,
`fileName`-д `{{thisModule.headers.content-disposition}}`-с задалсан нэр
эсвэл тогтмол нэр өгнө.

## 4. Дата стор-ийн өөрчлөлт (шаардлагатай бол)

Одоогийн "User Conversations" data store-д зөвхөн `last_plan` (нэг л
бүрэн текст) хадгалдаг. Хэрэв ирээдүйд "зөвхөн нэг бүлэгт засвар хийх"
маягийн боломж нэмэх бол `chapter1_text`, `chapter3_text`,
`chapter4_text`, `chapter5_text` гэсэн тусдаа талбарууд нэмэхийг
зөвлөж байна. Одоохондоо энгийн байдлаар **бүх төсвийг дахин бичих**
(одоогийн 36 модулийн засварын логиктой адил, гэхдээ 4 бүлгийн Claude
дуудлагаар) хандлагыг санал болгож байна — учир нь энэ нь одоогийн
ажиллаж байгаа засварын урсгалтай хамгийн ойрхон, эрсдэл багатай.

## 5. Хугацаа, зардлын тооцоо

- 1 (тоо задлах) + 2 (finance/payroll) + 4 (бүлэг) + 1 (docx) = ойролцоогоор
  **8 дараалсан алхам**, тус бүр 10-40 секунд (web search ашигласан
  Claude дуудлага удаан байж болно) — нийт **3-6 минут** орчим.
- Энэ нь одоогийн "1-3 минутын" туршлагаас удаан тул module 19-д байгаа
  "Манай баг одоо энэ мэдээллийг боловсруулж байна, түр хүлээнэ үү"
  мессежийг хэвээр ашиглах нь чухал (хэрэглэгч хүлээх зэрэгтэй байх).
- Claude API-ийн зардал: 4 бүлгийн дуудлага (тус бүр ~4000-6000
  токен гаралттай, зарим нь web search ашигладаг тул нэмэлт зардал орно)
  — нэг төслийн зардал өмнөхөөс ойролцоогоор 3-4 дахин нэмэгдэнэ. Энэ
  нь одоогийн 25,000₮-ийн үнэд багтах эсэхийг тооцоолж үзэхийг зөвлөж
  байна.

## 6. Дараагийн алхам (танаас хэрэгтэй зүйл)

Энэ дизайныг бодит Make.com scenario дээр хэрэгжүүлэхийн тулд надад
дараах 2 зүйл хэрэгтэй:

1. **Render.com дээр deploy хийсэн бодит URL** (README.md-ийн 1-р хэсгийг
   дагаж хийгээд надад URL-ээ өгнө үү, жишээ нь
   `https://menejer-ai-plan-service-xxxx.onrender.com`) болон тохируулсан
   `API_KEY`.
2. **Баталгаажуулалт** — учир нь энэ бол одоо ажиллаж байгаа, төлбөр
   төлсөн хэрэглэгчидтэй bot тул би үүнийг live scenario дээр өөрчлөхийн
   өмнө таны зөвшөөрлийг авах ёстой (том өөрчлөлт учир туршилтгүйгээр шууд
   production дээр хийвэл эрсдэлтэй).

Эдгээр 2 зүйл бэлэн болмогц би Make.com scenario 6206912-г дээрх
дизайны дагуу шинэчилж, эхлээд туршиж, танд баталгаажуулаад идэвхжүүлнэ.
